import { describe, it, expect } from 'vitest'

describe.skip('當會員登入資料正確', () => {
  it('登入後，畫面沒有登入及註冊按鈕', () => {
    // Arrange

    // Act

    // Assert
  })
})


describe.skip('當會員登入帳號錯誤', () => {
  it('顯示剩餘嘗試次數並最終鎖定帳號', () => {
    // Arrange

    // Act

    // Assert
  })
})
